import * as actions from 'modules/actions'
import reducer from 'routes/RequestPreApproval/modules/procedureCost'

describe('(Module) RequestPreApproval/procedureCost', () => {
  it('Should have an initial state', () => {
    expect(typeof reducer).to.equal('function')
  })
})
